# MemeBank repository recovery ledger — 2026-08-07

## Scope

This ledger records the recovery of generated MemeBank repository source from the last ten days without overwriting legacy repositories or treating an archive as proof of remote publication.

## Recovered source carrier

The sealed bootstrap was generated on **2026-07-30/31** and revalidated on **2026-08-07**.

Archive SHA-256:

```text
75ef76937e9d2cf1c649127daec461a589e6bd55bc29e545d84dc4d28b66dfee
```

Static validation passed for JSON, TOML, YAML, JSON Schema, OpenAPI, TypeScript, shell syntax, topology, marketing invariants, and credential-pattern scanning. Rust and Flutter execution remain CI responsibilities where their toolchains are installed.

## Sealed source commits

| Recovered repository | Source commit | Disposition |
|---|---|---|
| `.github` | `234c45d3d9114a822aa4bf2bef7efc6450a57fec` | Do not overwrite the newer live organization repository |
| `mb-interfaces` | `5c1a7a85855a95c281a05089e43ce1bcd0615172` | Missing remote; publish when repository creation is available |
| `mb-clients` | `b1310e3a00c2168f9bca2fc1a59ac0b6faec2767` | Missing remote; publish when repository creation is available |
| `mb-cli` | `b5cbe797fa05f52f1a8c6f87bf811d88d4ab54c4` | Missing canonical remote; do not substitute empty `mbk-cli` |
| `memebank-api-server.rs` | `77283ca5d9bfd665a9c252ee8f0d5eecd6bf5a00` | Missing remote |
| `memebank-web-server.rs` | `a5f67c86cd0e3bcefbd190da3e8d291119af5f56` | Missing remote |
| `memebank-media-worker.rs` | `ee0422582ff4beebedf40b02b7fd2c324d856b68` | Missing remote |
| `memebank-flutter` | `ef5a1a86fdca6a3d5e12a2b9d4df1e2555d216da` | Migration source for newer canonical target `mbk-flutter`; do not create both |
| `mb-infra` | `7c068b24ebc3dce9a1874aa1c8328504e85010ec` | Missing remote; `memebank-infra` remains forbidden |
| `memebank-mcp-server.rs` | `77dbf5dd4d7620a6572b085ae6ed726343c230d1` | Missing remote |
| `memebank-e2e` | `a1c7282173072ca9c29a9188502e876bc68260e4` | Semantically recovered into the newer live repository through PR #3 |
| `memebank-monorepo` | `0aa9a914a7a83950f0f91f725f34e2457f065f81` | Missing remote; publish last after child revisions are reachable |
| `memebank.github.io` | `cadba1f33077f8a82803661386a0149ed1ed9b8c` | Older source; publish the newer live staging tree from `.github/marketing-site` instead |

## Current remote state

Verified live canonical or recovery-relevant repositories:

- `memebank/.github` — live, public, nonempty;
- `memebank/memebank-e2e` — live, private, nonempty;
- `memebank/mbk-cli` — live but empty and not the canonical `mb-cli` target.

The legacy repositories remain preserved: `mbk-api`, `mbk-rest-api`, `mbk-ocr-api`, `mbk-pwa`, `mbk-scripts`, `memebanc-blog`, and `stop-billing`.

## E2E semantic recovery

[`memebank-e2e#3`](https://github.com/memebank/memebank-e2e/pull/3) preserves the newer process-level Node suite and adds the recovered Rust cross-runtime conformance matrix. It records the original sealed source SHA rather than force-pushing unrelated history.

## Newer desktop naming decision

The August 6 desktop contract names:

- `memebank/mbk-desktop.rs` — planned; no generated source tree was found;
- `memebank/mbk-flutter` — planned; the recovered `memebank-flutter` tree is the migration source.

The `mbk-*` desktop naming supersedes creation of a second `memebank-flutter` remote. This does not rename the canonical CLI: the CLI target remains `mb-cli` unless a separate reviewed decision changes it.

## Publication blocker

The connected GitHub application can create branches, commits, pull requests, issues, and merges, but it does not expose organization repository creation. The execution runtime has no `gh` binary and blocks direct outbound network connections, so the supplied short-lived token could not be used safely for REST repository creation or Git pushes.

No credential was committed, pasted into this ledger, written into a remote URL, or printed to logs.

## Safe continuation

A publish-ready package was generated with:

- independent Git histories and verified Git bundles;
- an idempotent REST-plus-Git publisher that reads `GH_TOKEN` only from the environment;
- no-overwrite checks for nonempty destinations;
- `mbk-flutter` and monorepo naming migration commits;
- current `.github/marketing-site` migration for `memebank.github.io`; and
- publication order with `memebank-monorepo` last.

Completion still requires remote creation, first pushes, baseline PR/check evidence, governance, and synchronized Linear/GitHub Project records. Source carriers and local bundles are recovery evidence, not remote-publication evidence.
